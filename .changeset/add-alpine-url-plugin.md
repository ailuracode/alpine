---
"@ailuracode/alpine-url": minor
---

Add `@ailuracode/alpine-url` — reactive `$store.url` synced with the browser URL. Requires a Zod object schema (Zod 3 or 4) for typed `get`/`set`/`push`/`replace`, runtime validation, and a reactive `search` getter.
