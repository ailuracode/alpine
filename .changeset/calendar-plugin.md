---
"@ailuracode/alpine-calendar": minor
---

Add `@ailuracode/alpine-calendar` — Alpine.js calendar date logic powered by date-fns. Exposes `$calendar(options?)` magic for month navigation, selection (single, range, multiple), and grid data without rendering UI.
