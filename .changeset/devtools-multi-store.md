---
"@ailuracode/alpine-query-devtools": patch
---

Inspect multiple query clients in one devtools panel via `additionalStores` (e.g. `$store.query` plus headless `createQueryClient()` instances). Merged snapshots show adapter labels and route cache actions to the correct client.
