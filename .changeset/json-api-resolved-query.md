---
"@ailuracode/alpine-json-api": patch
---

Add automatic `relationships.*.resolved` hydration from compound `included` documents and `jsonApiQueryOptions()` / `jsonApiFindOneQueryOptions()` helpers for `$store.query.observe()`.
